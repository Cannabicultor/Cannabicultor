# Cannabicultor

Indexed in codebase-memory as project `Users-ernie-cannabicultor`.

For architecture, callers, impact analysis, or "where is this handled", use `codebase-memory-mcp` first (`search_graph`, `trace_path`, `get_architecture`, `detect_changes`). Do not start with repo-wide grep.

Related indexed projects: `Users-ernie-cannabis-data-pipeline`, `cannabis-pedigree`, `Users-ernie-cannabicultor-admin`, `kb-rag-cannabicultor`.
